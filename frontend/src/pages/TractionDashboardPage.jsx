import { useEffect, useMemo, useState } from 'react';
import { useLocation } from 'react-router-dom';
import { AlertTriangle, CheckCircle2, Database, Link2, Plus, ShieldCheck, Zap, X } from 'lucide-react';
import { fetchAllScoredJobs, fetchWeeklyPlan } from '../services/api';

const ORANGE = '#C9842A';
const PURPLE = '#6B5B95';
const fields = [
  ['job_id', 'Job ID', 'TRC-NEW-001'], ['asset_id', 'Asset ID', 'OHE-PUNE-001'],
  ['asset_type', 'Asset Type', 'OHE'], ['section_id', 'Section', 'PUNE-LNL'],
  ['track_id', 'Track', 'PUNE-LNL-UP'], ['location_km', 'Location (KM)', '227.4'],
  ['defect_type', 'Maintenance / Defect', 'OHE_FAULT'], ['estimated_duration_hours', 'Duration (hours)', '2'],
];

const badge = (level) => level === 'CRITICAL'
  ? 'bg-[#C92A2A]/10 text-[#C92A2A] border-[#C92A2A]/30'
  : level === 'HIGH'
    ? 'bg-[#F08C00]/10 text-[#F08C00] border-[#F08C00]/30'
    : 'bg-[#FFF6E8] text-[#9A641D] border-[#E9C98F]';

export default function TractionDashboardPage() {
  const location = useLocation();
  const [jobs, setJobs] = useState([]);
  const [weekly, setWeekly] = useState(null);
  const [loading, setLoading] = useState(true);
  const [showAdd, setShowAdd] = useState(false);
  const [submitted, setSubmitted] = useState([]);

  useEffect(() => {
    Promise.all([fetchAllScoredJobs(), fetchWeeklyPlan()])
      .then(([all, plan]) => { setJobs((all || []).filter(j => j.department === 'Traction')); setWeekly(plan); })
      .catch(err => console.error('Traction console load failed:', err))
      .finally(() => setLoading(false));
  }, []);

  const ownJobs = useMemo(() => { const map = new Map(); [...jobs, ...submitted].forEach(j => map.set(j.job_id, j)); return [...map.values()]; }, [jobs, submitted]);
  const blocks = weekly?.blocks || weekly?.scheduled_blocks || [];
  const ownBlocks = blocks.filter(b => (b.departments || []).includes('Traction'));
  const sharedBlocks = ownBlocks.filter(b => b.is_consolidated || (b.departments || []).length > 1);
  const ghostBlocks = blocks.filter(b => !(b.departments || []).includes('Traction')).slice(0, 5);
  const critical = ownJobs.filter(j => j.criticality_level === 'CRITICAL').length;
  const pending = ownJobs.filter(j => (j.status || 'PENDING') === 'PENDING').length;
  const path = location.pathname;
  const title = path.includes('/jobs/add') ? 'Add Maintenance Job' : path.includes('/jobs/pending') ? 'Pending / Intake' : path.includes('/jobs/priority-risk') ? 'Priority & Risk' : path.includes('/schedule') ? 'My Weekly Schedule' : path.includes('/blocks') ? 'Upcoming Blocks' : path.includes('/assets/ohe') ? 'OHE Assets' : path.includes('/assets/power') ? 'Power Supply / Isolation' : path.includes('/assets') ? 'Asset Risk' : path.includes('/shared-blocks') ? 'Shared Blocks' : path.includes('/dependencies') ? 'Dependencies' : path.includes('/engineering-coordination') ? 'Engineering Coordination' : path.includes('/snt-coordination') ? 'S&T Coordination' : path.includes('/ohe-safety') ? 'OHE / Power Block Safety' : path.includes('/isolation') ? 'Isolation & Restoration' : 'Traction Control Centre';

  const submitJob = (e) => {
    e.preventDefault();
    const values = Object.fromEntries(new FormData(e.currentTarget).entries());
    setSubmitted(current => [...current, { ...values, department: 'Traction', status: 'PENDING', location_km: Number(values.location_km || 0), estimated_duration_hours: Number(values.estimated_duration_hours || 2), ai_priority_score: null }]);
    setShowAdd(false);
    e.currentTarget.reset();
  };

  if (loading) return <main className="p-5 bg-[#F4F6F8] min-h-full"><div className="bg-white border border-[#D6DEE6] p-6 rounded text-sm text-[#52606D]">Loading Traction operational data…</div></main>;
  const jobTable = ownJobs.slice(0, path.includes('/jobs') ? 50 : 8);
  const viewJobs = title === 'Dependencies' ? ownJobs.filter(j => j.dependency_job_id) : title === 'Shared Blocks' ? sharedBlocks : ownJobs;

  return <main className="p-4 space-y-4 bg-[#F4F6F8] min-h-full font-sans text-[#1F2933]">
    <section className="bg-white border border-[#D6DEE6] rounded shadow-sm p-4 flex flex-col lg:flex-row lg:items-center lg:justify-between gap-3 border-l-4" style={{borderLeftColor: ORANGE}}>
      <div className="flex items-center gap-3"><div className="h-11 w-11 rounded flex items-center justify-center text-white" style={{backgroundColor: ORANGE}}><Zap className="h-6 w-6"/></div><div><div className="flex items-center gap-2"><h1 className="text-lg font-black uppercase tracking-wide">Traction Control Centre</h1><span className="px-2 py-0.5 rounded bg-[#1E3A5F] text-white text-[9px] font-bold font-mono">TRD CONSOLE</span></div><p className="text-xs text-[#52606D] mt-1">Sr. DEE (TRD) / Pune • Data feed: Traction Data Management System (TDMS)</p></div></div><button onClick={() => setShowAdd(true)} className="inline-flex items-center justify-center gap-2 px-3 py-2 rounded text-xs font-bold text-white" style={{backgroundColor: ORANGE}}><Plus className="h-4 w-4"/> Add Maintenance Job</button></section>
    {title !== 'Traction Control Centre' && <div className="bg-[#FFF6E8] border border-[#E9C98F] rounded px-3 py-2 text-xs"><span className="font-bold" style={{color: ORANGE}}>Traction / {title}</span><span className="text-[#52606D]"> — department-specific operational view</span></div>}
    <section className="grid grid-cols-2 xl:grid-cols-5 gap-3">{[["Traction Jobs", ownJobs.length, ORANGE, 'TDMS + submitted'], ['Critical', critical, '#C92A2A', 'Immediate attention'], ['Pending / Intake', pending, '#F08C00', 'Awaiting planning'], ['Scheduled Blocks', ownBlocks.length, '#2F9E44', 'Unified weekly plan'], ['Shared Blocks', sharedBlocks.length, PURPLE, 'Multi-department']].map(([label,value,color,sub]) => <div key={label} className="bg-white border border-[#D6DEE6] rounded p-3 border-t-2" style={{borderTopColor: color}}><p className="text-[10px] uppercase font-bold text-[#52606D]">{label}</p><p className="text-2xl font-black font-mono mt-1" style={{color}}>{value}</p><p className="text-[10px] text-[#718294] mt-1">{sub}</p></div>)}</section>
    {(title === 'Traction Control Centre' || title === 'Priority & Risk' || title === 'Pending / Intake' || title === 'Add Maintenance Job') && <>
      <section className="grid xl:grid-cols-3 gap-4"><div className="xl:col-span-2 bg-white border border-[#D6DEE6] rounded overflow-hidden"><div className="px-3 py-2 bg-[#F4F6F8] border-b border-[#D6DEE6] flex justify-between"><h2 className="text-xs font-bold uppercase tracking-wider">Traction Maintenance Queue & Risk</h2><span className="text-[10px] font-mono text-[#52606D]">XGBoost Priority 0–100</span></div><div className="overflow-x-auto"><table className="w-full text-left text-xs"><thead className="bg-[#F8FAFB] text-[9px] uppercase text-[#60748A]"><tr>{['Job ID','Asset / Defect','Location','Criticality','AI Risk','Status'].map(x=><th key={x} className="px-3 py-2">{x}</th>)}</tr></thead><tbody className="divide-y divide-[#E4E9EE]">{jobTable.map(j=><tr key={j.job_id} className="hover:bg-[#F8FAFB]"><td className="px-3 py-2 font-mono font-bold text-[#1E3A5F]">{j.job_id}</td><td className="px-3 py-2"><div className="font-semibold">{j.asset_type || 'OHE'} / {String(j.defect_type || 'MAINTENANCE').replaceAll('_',' ')}</div><div className="text-[10px] text-[#718294]">{j.asset_id}</div></td><td className="px-3 py-2 font-mono">KM {j.location_km}</td><td className="px-3 py-2"><span className={`px-1.5 py-0.5 rounded border text-[9px] font-bold ${badge(j.criticality_level)}`}>{j.criticality_level || 'MEDIUM'}</span></td><td className="px-3 py-2 font-mono font-bold text-[#1E3A5F]">{j.ai_priority_score == null ? 'Pending' : Number(j.ai_priority_score).toFixed(1)}</td><td className="px-3 py-2 text-[9px] font-mono font-bold">{j.status || 'PENDING'}</td></tr>)}</tbody></table></div></div><div className="bg-white border border-[#D6DEE6] rounded overflow-hidden"><div className="px-3 py-2 bg-[#F4F6F8] border-b border-[#D6DEE6]"><h2 className="text-xs font-bold uppercase tracking-wider">Attention Required</h2></div><div className="p-3 space-y-2">{ownJobs.filter(j=>j.criticality_level==='CRITICAL'||j.status==='DEFERRED').slice(0,6).map(j=><div key={j.job_id} className="p-2 border border-[#E4E9EE] rounded flex gap-2"><AlertTriangle className="h-4 w-4 text-[#C92A2A] shrink-0"/><div><p className="text-[11px] font-bold">{j.job_id} • {j.criticality_level}</p><p className="text-[10px] text-[#60748A]">{j.defect_type?.replaceAll('_',' ')} at KM {j.location_km}</p></div></div>)}{!ownJobs.some(j=>j.criticality_level==='CRITICAL'||j.status==='DEFERRED')&&<p className="text-xs text-[#2F9E44] flex gap-2"><CheckCircle2 className="h-4 w-4"/> No critical/deferred Traction items in current feed.</p>}</div></div></section>
      <section className="grid xl:grid-cols-2 gap-4"><div className="bg-white border border-[#D6DEE6] rounded overflow-hidden"><div className="px-3 py-2 bg-[#F4F6F8] border-b border-[#D6DEE6]"><h2 className="text-xs font-bold uppercase tracking-wider">OHE / Traction Asset Watch</h2></div><div className="grid grid-cols-2 gap-3 p-3"><div className="border border-[#D6DEE6] rounded p-3"><Zap className="h-5 w-5 mb-2" style={{color:ORANGE}}/><p className="text-xs font-bold">OHE Assets</p><p className="text-xl font-black font-mono mt-1">{ownJobs.filter(j=>String(j.asset_type||'').toUpperCase().includes('OHE')).length}</p><p className="text-[10px] text-[#718294]">Jobs in OHE domain</p></div><div className="border border-[#D6DEE6] rounded p-3"><ShieldCheck className="h-5 w-5 mb-2" style={{color:ORANGE}}/><p className="text-xs font-bold">Power / Isolation</p><p className="text-xl font-black font-mono mt-1">{ownJobs.filter(j=>String(j.defect_type||'').toUpperCase().includes('POWER') || String(j.safety_conflict_tag||'').toUpperCase().includes('ISOLATION')).length}</p><p className="text-[10px] text-[#718294]">Power-block related jobs</p></div></div></div><div className="bg-white border border-[#D6DEE6] rounded overflow-hidden"><div className="px-3 py-2 bg-[#F4F6F8] border-b border-[#D6DEE6] flex justify-between"><h2 className="text-xs font-bold uppercase tracking-wider">TDMS Feed Health</h2><span className="text-[9px] px-1.5 py-0.5 rounded bg-[#FFF6E8] text-[#9A641D] font-bold">CONNECTED</span></div><div className="p-3 grid grid-cols-3 gap-2 text-center"><div><p className="text-lg font-black font-mono">{ownJobs.length}</p><p className="text-[9px] text-[#718294]">TDMS Jobs</p></div><div><p className="text-lg font-black font-mono">{weekly?.status || 'READY'}</p><p className="text-[9px] text-[#718294]">Weekly Solver</p></div><div><p className="text-lg font-black font-mono">{ownBlocks.length}</p><p className="text-[9px] text-[#718294]">Blocks</p></div></div></div></section>
    </>}
    {['My Weekly Schedule','Upcoming Blocks','Shared Blocks','Dependencies','Asset Risk','OHE Assets','Power Supply / Isolation','Engineering Coordination','S&T Coordination','OHE / Power Block Safety','Isolation & Restoration'].includes(title) && <section className="bg-white border border-[#D6DEE6] rounded overflow-hidden"><div className="px-3 py-2 bg-[#F4F6F8] border-b border-[#D6DEE6] flex items-center justify-between"><h2 className="text-xs font-bold uppercase tracking-wider">{title}</h2><span className="text-[10px] text-[#52606D]">Unified MARS view • Traction scope</span></div><div className="overflow-x-auto"><table className="w-full text-left text-xs"><thead className="bg-[#F8FAFB] text-[9px] uppercase text-[#60748A]"><tr><th className="px-3 py-2">Block / Job</th><th className="px-3 py-2">Section / Track</th><th className="px-3 py-2">Window</th><th className="px-3 py-2">Departments</th><th className="px-3 py-2">State</th></tr></thead><tbody className="divide-y divide-[#E4E9EE]">{viewJobs.slice(0,20).map((x,i)=><tr key={x.block_id||x.job_id||i}><td className="px-3 py-2 font-mono font-bold text-[#1E3A5F]">{x.block_id||x.job_id}</td><td className="px-3 py-2">{x.section_id} / {x.track_id}</td><td className="px-3 py-2 font-mono text-[10px]">{x.start_time?.replace('T',' ') || x.due_date || '—'}{x.end_time ? ` → ${x.end_time.replace('T',' ')}` : ''}</td><td className="px-3 py-2">{(x.departments||[x.department]).join(', ')}</td><td className="px-3 py-2 font-bold text-[10px]">{x.status || (x.is_consolidated?'SHARED / PURPLE':'SCHEDULED')}</td></tr>)}</tbody></table></div></section>}
    {ghostBlocks.length > 0 && (title === 'Traction Control Centre' || title === 'Shared Blocks') && <section className="bg-white border border-[#D6DEE6] rounded overflow-hidden"><div className="px-3 py-2 bg-[#F4F6F8] border-b border-[#D6DEE6] flex items-center gap-2"><Link2 className="h-4 w-4" style={{color:PURPLE}}/><h2 className="text-xs font-bold uppercase tracking-wider">Cross-Department Shared Block Opportunities</h2></div><div className="p-3 grid md:grid-cols-2 xl:grid-cols-3 gap-2">{ghostBlocks.map(b=><div key={b.block_id} className="border border-[#D6DEE6] rounded p-3 bg-[#FAFBFC]"><p className="text-[10px] font-mono font-bold text-[#52606D]">{b.block_id} • GHOST</p><p className="text-xs font-bold mt-1">{(b.departments||[]).join(', ') || 'Other Department'}</p><p className="text-[10px] text-[#718294] mt-1">{b.section_id} / {b.track_id} • {b.duration_hours || 2}h</p></div>)}</div></section>}
    {showAdd && <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#0B1F33]/50 p-4"><div className="w-full max-w-3xl max-h-[92vh] overflow-y-auto bg-white rounded shadow-2xl border border-[#D6DEE6]"><div className="px-4 py-3 text-white flex items-center justify-between" style={{backgroundColor:'#1E3A5F'}}><div><h2 className="text-sm font-black uppercase tracking-wide">Add Traction Maintenance Job</h2><p className="text-[10px] text-white/75 mt-0.5">Submit to the Unified Job Pool • Planner schedules the block</p></div><button type="button" onClick={()=>setShowAdd(false)}><X className="h-5 w-5"/></button></div><form onSubmit={submitJob} className="p-4 space-y-4"><div className="grid grid-cols-1 md:grid-cols-2 gap-3">{fields.map(([name,label,placeholder])=><label key={name} className="block"><span className="block text-[10px] font-bold uppercase tracking-wide text-[#52606D] mb-1">{label}</span><input name={name} defaultValue={name==='job_id'?'':undefined} placeholder={placeholder} required className="w-full rounded border border-[#D6DEE6] bg-white px-2.5 py-2 text-xs text-[#1F2933] outline-none focus:border-[#C9842A]" type={name.includes('location')||name.includes('duration')?'number':'text'} step={name.includes('location')||name.includes('duration')?'0.1':undefined}/></label>)}</div><div className="grid grid-cols-1 md:grid-cols-2 gap-3"><label><span className="block text-[10px] font-bold uppercase text-[#52606D] mb-1">Criticality</span><select name="criticality_level" defaultValue="HIGH" className="w-full rounded border border-[#D6DEE6] px-2.5 py-2 text-xs"><option>CRITICAL</option><option>HIGH</option><option>MEDIUM</option><option>LOW</option></select></label><label><span className="block text-[10px] font-bold uppercase text-[#52606D] mb-1">Due Date</span><input name="due_date" type="date" defaultValue="2026-09-15" required className="w-full rounded border border-[#D6DEE6] px-2.5 py-2 text-xs"/></label><label><span className="block text-[10px] font-bold uppercase text-[#52606D] mb-1">Preferred Window</span><select name="preferred_window" defaultValue="NIGHT" className="w-full rounded border border-[#D6DEE6] px-2.5 py-2 text-xs"><option>NIGHT</option><option>DAY</option><option>ANY</option></select></label><label><span className="block text-[10px] font-bold uppercase text-[#52606D] mb-1">Machine Required</span><select name="machine_required" defaultValue="NO" className="w-full rounded border border-[#D6DEE6] px-2.5 py-2 text-xs"><option>NO</option><option>YES</option></select></label><label><span className="block text-[10px] font-bold uppercase text-[#52606D] mb-1">Dependency Job ID</span><input name="dependency_job_id" placeholder="e.g. ENG-PLN-044" className="w-full rounded border border-[#D6DEE6] px-2.5 py-2 text-xs"/></label><label><span className="block text-[10px] font-bold uppercase text-[#52606D] mb-1">Safety Conflict Tag</span><select name="safety_conflict_tag" defaultValue="NORMAL" className="w-full rounded border border-[#D6DEE6] px-2.5 py-2 text-xs"><option>NORMAL</option><option>INTERLOCKING</option><option>LEVEL_CROSSING</option><option>TELECOM_ISOLATION</option></select></label></div><div className="flex justify-end gap-2 pt-2 border-t border-[#E4E9EE]"><button type="button" onClick={()=>setShowAdd(false)} className="px-3 py-2 rounded border border-[#D6DEE6] text-xs font-bold text-[#52606D]">Cancel</button><button type="submit" className="px-3 py-2 rounded text-white text-xs font-bold" style={{backgroundColor:ORANGE}}>Submit Job to Intake</button></div></form></div></div>}
  </main>;
}
